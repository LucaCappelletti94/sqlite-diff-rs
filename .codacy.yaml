---
exclude_paths:
  - "benches/**"
  - "compile-bench/**"
  - "fuzz/**"
# Tests duplicate by design; every other tool and metric still covers them.
engines:
  duplication:
    exclude_paths:
      - "integration-tests/**"
      - "tests/**"
